import { LoginForm } from '@/components/features/auth/login-form'

export const metadata = {
  title: 'Login | SimpleCRM',
  description: 'Sign in to your SimpleCRM account',
}

export default function LoginPage() {
  return <LoginForm />
}
