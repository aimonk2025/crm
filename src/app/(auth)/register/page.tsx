import { RegisterForm } from '@/components/features/auth/register-form'

export const metadata = {
  title: 'Register | SimpleCRM',
  description: 'Create your SimpleCRM account',
}

export default function RegisterPage() {
  return <RegisterForm />
}
